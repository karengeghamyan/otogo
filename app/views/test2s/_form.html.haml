= form_for @test2 do |f|
  -if @test2.errors.any?
    #error_explanation
      %h2= "#{pluralize(@test2.errors.count, "error")} prohibited this test2 from being saved:"
      %ul
        - @test2.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
